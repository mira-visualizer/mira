import React, {Component} from "react";

class Side_Panel extends Component {
  render() {
    return(
      <div id="sidePanel">
        {/* <p>{JSON.stringify(this.props.regionData)}</p> */}
      </div>
    )
  }
}

export default Side_Panel;